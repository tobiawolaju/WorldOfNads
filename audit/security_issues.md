| Issue ID | Description | Location / File | Status | Suggested Fix |
| --- | --- | --- | --- | --- |
| SEC-001 | WebSocket gameplay messages are unauthenticated; any client can send `update_state`, `pickup_request`, and `drop_request` without proof of identity. | backend/index.js (gameWss `message` handler) | Pending | Require authenticated session tokens (Privy/JWT) per connection and reject unauthenticated messages; bind playerId to authenticated user. |
| SEC-002 | Movement speed clamp is per message using a fixed dt, so a client can spam `update_state` to move faster than `MAX_PLAYER_SPEED`. | backend/index.js (`update_state` handling) | Pending | Track `lastUpdateAt` per player, clamp based on real elapsed time, and rate-limit or drop excess updates. |
| SEC-003 | Client controls full position including `y`, enabling vertical movement/teleporting within speed clamps; no server-side bounds or collision checks. | backend/index.js (`update_state` handling) | Pending | Enforce server-side bounds (floor/ceiling), validate `y` against allowed ranges, or simulate movement server-side. |
| SEC-004 | No rate limiting or max message size on WebSocket or events stream; attackers can flood messages and event logs. | backend/index.js (gameWss/eventsWss) | Pending | Add per-connection rate limits, message size caps, and disconnect/ban on abuse. |
| SEC-005 | Winner is determined solely by current `chicken.holderId`; a holder can disconnect at match end to force "no winner" and block payouts. | backend/index.js (`resolveRoundWinner`, `close` handler) | Pending | Add a grace period and remember last valid holder; settle based on last holder within a time window. |
| SEC-006 | Username is client-supplied via query param and used to fetch wallet for payouts; identity can be spoofed. | backend/index.js (connection), backend/firebaseClient.js (`getPlayerWallet`) | Pending | Authenticate users and bind a stable user ID to the connection; fetch wallet by UID rather than username. |
| SEC-007 | Backend uses Firebase client SDK with public config; if RTDB rules are permissive, attackers can tamper with `matches`, `users`, or `rewards`. | backend/firebaseClient.js | Pending | Use Firebase Admin SDK with service account on the server and enforce strict database rules; validate all updates server-side. |
| SEC-008 | Match selection for settlement is based on "upcoming/live" status in Firebase without binding to this server's match instance; could settle the wrong match if DB is manipulated. | backend/index.js (`findActiveMatch` usage) | Pending | Bind the server to a specific matchId at start, verify match ownership, and require server-side authorization for status updates. |
| SEC-009 | On-chain settlement relies entirely on a single trusted authority key; no on-chain checks that `expectedParticipants` matches or that the match has started. | contracts/src/WONSponsorArenaEscrow.sol (`settleMatch`) | Pending | Enforce `block.timestamp >= startTime`, `participants.length == expectedParticipants`, and consider multisig/timelock for trusted authority. |
| SEC-010 | Trusted authority can be changed instantly by owner with no delay, increasing takeover risk. | contracts/src/WONSponsorArenaEscrow.sol (`setTrustedAuthority`) | Pending | Require timelock or multisig for authority changes and emit/restrict changes with cooldown. |
| SEC-011 | Client-side stop-match control could allow players to end rounds early if enabled. | backend/index.js (WebSocket message handling) | Fixed | Remove any client stop-match message type; only the server timer ends rounds. |
| SEC-012 | Six of seven Solidity files (WONsMatchEngine.sol, WONsLootBox.sol, WONsBridge.sol, WONsRewards.sol, WONsRegistry.sol, WONsSkins.sol) are plain-text design notes, not compilable Solidity code. Only WONSponsorArenaEscrow.sol is a real, deployable contract. | contracts/src/ | Pending | Implement all six contracts as real Solidity with complete logic, tests, and testnet deployment before mainnet. |
| SEC-013 | Zero automated tests exist anywhere in the repository. No unit tests, integration tests, or test harness for backend, contracts, or client. A system handling real money has no regression safety net. | entire repository | Pending | Add unit tests for contractClient.js and firebaseClient.js; add Foundry tests for all Solidity contracts; add CI pipeline running tests on push. |
| SEC-014 | Toast/notification system was missing `<ToastContainer />` from react-toastify — all `toast()` calls were silently queued and never rendered. | frontend/src/App.tsx, frontend/src/components/ui/custom-toast.tsx | Fixed | Added `<ToastContainer />` to App.tsx render tree (z-index 9999, above all modals). Wired up `closeToast` prop on CustomToast close button instead of `toast.dismiss()` (was dismissing ALL toasts). |
| SEC-015 | Match creation in sponsor dashboard silently failed when "time too close" (e.g. 5 min) — contract reverted but user saw no notification because ToastContainer was missing. | frontend/src/pages/SpounsorDashbaord.jsx | Fixed | Added frontend-side validation requiring match start time ≥1 hour from now. Combined with ToastContainer fix, contract revert errors now display as error toasts. |
| SEC-016 | Numerous user-triggered operations across the site had no success/error feedback (skin EQUIP, skin MINT, PLAY tracking, admin role toggles, skin creation, contract withdraw, analytics export, waitlist email, referral copy, wallet address copy). | frontend/src/pages/Dashboard.tsx, AdminUsers.tsx, AdminSkins.tsx, AdminContracts.tsx, AdminAnalytics.tsx, components/waitlist/EmailCapture.tsx, ReferralPlaceholder.tsx, IdCard.tsx | Fixed | Added `showSuccessToast`/`showErrorToast` calls to all identified operations. Uses the unified toast notification system that now renders above all modals. |
| SEC-017 | WebSocket token expiry was 30 seconds (`SESSION_TOKEN_EXPIRY_MS = 30000`) — browser takes 10-60s to download/compile Godot WASM, so the auth token expired before the WebSocket connection was even attempted. | backend/index.js | Fixed | Increased token expiry to 5 minutes (300000ms). |
| SEC-018 | WebSocket reconnect used the same already-consumed single-use auth token, causing server to reject with `invalid_token` on every reconnect attempt. | godot/scripts/PlayerManager.gd | Fixed | `_attempt_reconnection()` now strips the `TOKEN:` prefix from `local_username` before building the reconnect URL, falling back to bare-username connections on reconnect. |
| SEC-019 | WebSocket disconnect had no close code/reason logging — impossible to diagnose why the server was closing connections. | godot/scripts/PlayerManager.gd | Fixed | Added `ws.get_close_code()` and `ws.get_close_reason()` to the `STATE_CLOSED` log output. |
| IMPL-001 | Toast + inline status message redundancy — AdminSkins, AdminContracts, AdminUsers show both a toast AND an inline `<span>` for the same event. The toast fades after 5s while the inline persists. Redundant but harmless. | frontend/src/pages/AdminSkins.tsx, AdminContracts.tsx, AdminUsers.tsx | Info | Toast provides temporary notice, inline provides persistent reference. Acceptable dual-channel feedback. |
| IMPL-002 | Hardcoded 1-hour minimum in match creation frontend validation may not match the contract's actual minimum start time. If contract uses a different buffer (e.g. 30 min or 24h), validation is inconsistent. | frontend/src/pages/SpounsorDashbaord.jsx (line 217) | Pending | Derive minimum from contract config or env variable instead of hardcoding. |
| IMPL-003 | `closeToast` prop is injected by react-toastify when rendering custom components, but the component signature marks it as optional (`closeToast?: () => void`). If react-toastify fails to pass it (edge case), the close button silently does nothing. | frontend/src/components/ui/custom-toast.tsx (line 10) | Pending | Add a fallback: if `closeToast` is undefined, log a warning and attempt `toast.dismiss()` with the toast ID (not without args). |
| IMPL-004 | Token-stripping on reconnect uses `find(":")` which splits on the first colon. If a username legitimately contains a colon (uncommon for social logins but possible), the wrong part would be stripped and the username would be corrupted. | godot/scripts/PlayerManager.gd (line 355) | Info | Currently safe because Privy social usernames don't contain colons and the token format is a fixed-length hex string. |
