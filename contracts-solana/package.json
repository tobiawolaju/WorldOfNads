{
  "name": "world-of-nads-solana",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "anchor build",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:mainnet": "anchor deploy --provider.cluster mainnet",
    "test": "anchor test --skip-deploy",
    "init-all": "ts-node scripts/init-all.ts",
    "lint": "prettier --check programs/",
    "format": "prettier --write programs/"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^2.0.0",
    "bs58": "^5.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.7",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.0"
  }
}
